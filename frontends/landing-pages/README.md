# Landing pages

Primary marketing site lives in `frontends/exchange-os` (Next.js). Add static campaign landings here as they are split from the main app.

Reference: `ai/knowledge-base/whichway-docs` and OpenClaw `content/` trees.
