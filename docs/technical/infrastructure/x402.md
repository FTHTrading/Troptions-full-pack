---
layout: default
title: x402 (Optional)
permalink: /infrastructure/x402/
---

# x402 / Apostle

**Production mesh:** [UnyKorn-X402-aws](https://github.com/FTHTrading/UnyKorn-X402-aws) (**public**) — LIVE on AWS + Cloudflare. Health: `https://x402.unykorn.org/health`. Pay-per-request for AI agents (HTTP 402, ATP, `X-Payment-Proof`) — not API keys.

**Monorepo (`main`):** lightweight sidecar at `backend/x402-gateway/` (port **4020**) plus L1/DAO hooks — for local staging and `X402_UPSTREAM` proxy to UnyKorn. **Not** the same deployment as `x402.unykorn.org`.

See **[X402_INTEGRATION.md](../X402_INTEGRATION.html)** for wiring, PM2, and honesty labels.

## What exists on main today

| Piece | Location |
|-------|----------|
| Sidecar gateway | `backend/x402-gateway/main.py` |
| L1 agent registry RPC | `l1/crates/rpc/src/x402.rs` |
| DAO x402 middleware | `backend/dao-service/x402_middleware.py` |
| FTH academy lab content | `x402-payments` lab in fth-academy |

## Live UnyKorn surfaces

| Surface | URL |
|---------|-----|
| Health | https://x402.unykorn.org/health |
| Digital twin | https://twin.unykorn.org |
| API docs | https://x402api.unykorn.org |

## Truth labels (2026-05-21)

| Label | Status |
|-------|--------|
| `x402_PUBLIC_FACILITATOR` | **CONFIRMED** (health curl) |
| `APOSTLE_CHAIN (public health)` | **CONFIRMED** (`chain_id: 7332`, operational) |
| `twin.unykorn.org` | **PENDING** if origin returns 522/timeout |

Optional client-specific branches (e.g. legacy `feature/x402-full-integration`) are **not** required for the public mesh narrative.

See also [`docs/investor/ONE_PAGER.md`](../investor/ONE_PAGER.html).
